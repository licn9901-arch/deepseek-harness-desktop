{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the packaged plugin recovery window can reach desktop recovery IPC",
  "windows": ["plugin-manager"],
  "permissions": []
}
